# API base URL (no trailing slash). Default: http://localhost:8000
# Vite dev server uses port 8080 (see vite.config.ts). The API must list that origin in
# CORS_ALLOWED_ORIGINS (defaults in apps/api/config/settings.py include 8080 and 5173).
VITE_API_URL=http://localhost:8000
