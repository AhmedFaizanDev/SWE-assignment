# API (Backend)

This folder is for backend-related code: REST or GraphQL APIs, serverless functions, or any server-side logic.

When you add an API (e.g. Node/Express, Fastify, or serverless), put it here so the repo stays organized with:

- **`apps/frontend`** — React/Vite UI
- **`apps/api`** — Backend services and APIs
